# Production environment — used by `npm run build`
# Point this to your backend server once it's deployed (e.g. DigitalOcean, Railway, Render)
# Leave blank to use relative /api/v1 (only works if backend is on same domain)
VITE_API_URL=https://api.kampeni.net/api/v1
